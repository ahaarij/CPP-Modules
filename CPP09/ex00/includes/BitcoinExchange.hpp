#pragma once

#include <iostream>
#include <vector>

class   BitcoinExchange
{
    private:

    public:
        BitcoinExchange();
        BitcoinExchange(const &BitcoinExchange copy);
        Bitcoin &operator=(const &BitcoinExchange copy);
        ~BitcoinExchange();

    // ashdui

    //i use map
}